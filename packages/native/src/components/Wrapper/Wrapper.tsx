import * as React from 'react';
import { Text, View } from 'react-native';

const Wrapper = (): JSX.Element => (
  <View>
    <Text>The Wrapper is called.</Text>
  </View>
);

export default Wrapper;
